spring:
  application:
    name: registry-service
  cloud:
    config:
      uri: http://localhost:2000
      fail-fast: true
      username: user
      password: 1234

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
